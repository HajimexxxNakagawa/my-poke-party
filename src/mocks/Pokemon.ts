import { Pokemon } from "@/types/Pokemon";

export const mockPokemon: Pokemon = {
  name: "ditto",
  img: "",
  type: ["normal"],
};

export const mockPokemonList = [mockPokemon, mockPokemon, mockPokemon];
